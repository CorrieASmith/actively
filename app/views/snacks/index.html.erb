<h3>Snacks</h3>
<br>

  <ul>
    <% @snacks.each do |snack| %>
      <% if current_user == snack.user %>
        <li><%= link_to snack.add_food, snack_path(snack) %></li>
      <% end %>
    <% end %>
  </ul>

<%= link_to 'Add Food', new_snack_path, id: "new_snack", remote: true %>
