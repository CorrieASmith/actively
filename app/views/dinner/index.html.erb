<h3>Dinner</h3>
<br>

<ul>
  <% @dinners.each do |dinner| %>
  <li><%= link_to dinner.add_food, breakfast_path(dinner) %></li>
  <% end %>
</ul>

<%= link_to 'Add Food', new_dinner_path, id: "new_dinner", remote: true %>
